# Flutter runtime configuration
# Pass these with --dart-define when running or building the app.
SUPABASE_URL=https://your-project-ref.supabase.co
SUPABASE_PUBLISHABLE_KEY=your-supabase-publishable-key

# User OpenAI/Anthropic keys are configured in the app and stored locally.
# Never put SUPABASE_SERVICE_ROLE_KEY in a Flutter, web, desktop, or mobile build.
