## ✅ MIGRÉ → module-7-social/messagerie.md

- ((problème central : la messagerie devient une couche d'interruption permanente))
- ((quand le chat est le bon canal vs quand il faut sortir vers email, tâche, note ou réunion))
- ((cadre de décision : répondre vite / snoozer / sortir / ignorer))
- ((Beeper = recommandation principale actuelle pour centralisation))
- ((Texts devient Beeper ; valeur surtout héritée / locale, pas recommandation de tête))
- ((iMessage : ne pas promettre une solution simple sur Windows ; AirMessage reste un hack niche avec Mac relais))
- ((Caprine : usage spécifique Messenger, secondaire))
- ((règles simples : une seule couche de notifications, peu de sons, extraction des actions et décisions))
- ((workflow recommandé : Beeper + notifications centralisées + traitement par lots))

## 🔶 À TRIER

- anciens dumps Texts trop affirmatifs sur iMessage Windows à ne pas réinjecter tels quels
