name: voiceflowz
description: "VoiceFlowz Flutter + Supabase migration baseline."
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ^3.11.3

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  flutter_riverpod: ^3.0.3
  go_router: ^16.2.5
  supabase_flutter: ^2.10.4
  flutter_secure_storage: ^10.0.0-beta.4
  permission_handler: ^12.0.1
  record: ^6.1.2
  speech_to_text: ^7.3.0
  uuid: ^4.5.1
  crypto: ^3.0.7

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
